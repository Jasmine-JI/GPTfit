/**
 * 升降冪
 */
export enum SortDirection {
  asc = 1,
  desc,
}

/**
 * 運動數據排序類別
 */
export enum ComplexSportSortType {
  nickname = 1,
  avgHeartRateBpm,
  calories,
  avgSpeed,
  totalDistanceMeters,
  cadence,
  power,
}
