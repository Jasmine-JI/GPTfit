/**
 * 登入方式類別
 */
export enum SignTypeEnum {
  email = 1,
  phone,
  token,
}

/**
 * 帳號類別
 */
export enum AccountTypeEnum {
  email = 1,
  phone,
}

/**
 * 帳號狀態
 */
export enum AccountStatusEnum {
  unenabled = 1,
  enabled,
}
